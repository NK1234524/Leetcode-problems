class Leetcode_10 {
    public double myPow(double x, int n) {
        
        if(n>=0){
         x =  Math.pow(x,n);
        }
        else{
           x = Math.pow(x,n);
        }
        return x;
    }
}